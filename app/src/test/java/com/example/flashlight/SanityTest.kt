package com.example.flashlight

import org.junit.Test
import org.junit.Assert.*

class SanityTest {
    @Test
    fun testPlaceholder() {
        // This test exists just to satisfy Gradle's "failOnNoDiscoveredTests"
        // until we write real DNS logic tests.
        assertEquals(4, 2 + 2)
    }
}